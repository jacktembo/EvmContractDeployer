# ============================================================================
# EVM Smart Contract Deployment Platform - Environment Variables
# ============================================================================
# 
# DEPLOYMENT INSTRUCTIONS FOR VPS:
# 1. Copy this file to .env in the root directory: cp .env.example .env
# 2. Update all values below with your actual credentials
# 3. For production deployment: Copy the .env file to your VPS
# 4. Never commit the .env file to version control (it's in .gitignore)
#
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL connection URL
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/evm_contracts

# ============================================================================
# BLOCKCHAIN INTEGRATION
# ============================================================================
# Etherscan API Key (Required for contract verification)
# Get your API key from: https://etherscan.io/myapikey
# Note: The same API key works for BSCScan, PolygonScan, and all EVM explorers
ETHERSCAN_API_KEY=YOUR_ETHERSCAN_API_KEY_HERE

# Reown (WalletConnect) Project ID (Required for wallet connections)
# Get your project ID from: https://cloud.reown.com
# IMPORTANT: Add your production domain to the allowed domains list at:
# https://cloud.reown.com - Go to your project → Settings → Domains
REOWN_PROJECT_ID=YOUR_REOWN_PROJECT_ID_HERE
VITE_REOWN_PROJECT_ID=YOUR_REOWN_PROJECT_ID_HERE

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
# Session secret for encryption
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=dev-secret-key-change-in-production

# Server port (default: 5000)
PORT=5000

# Node environment (development | production)
NODE_ENV=development
